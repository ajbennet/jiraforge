import { reduce } from './';
const heading = (node, schema) => {
  const result = [];
  node.forEach(n => {
    result.push(reduce(n, schema));
  });
  return result.join('');
};
export default heading;