import { reduce } from './';
const panel = (node, schema) => {
  const result = [];
  node.forEach(n => {
    result.push(reduce(n, schema));
  });
  return result.join('\n');
};
export default panel;