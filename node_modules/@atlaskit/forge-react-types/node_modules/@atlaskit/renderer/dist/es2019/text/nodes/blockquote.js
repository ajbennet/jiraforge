import { reduce } from './';
const blockquote = (node, schema) => {
  const result = [];
  node.content.forEach(n => {
    result.push(reduce(n, schema));
  });
  return `> ${result.join('')}`;
};
export default blockquote;