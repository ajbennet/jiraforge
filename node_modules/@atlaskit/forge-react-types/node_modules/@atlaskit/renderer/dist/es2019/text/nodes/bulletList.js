import { reduce } from './';
const bulletList = (node, schema) => {
  const result = [];
  node.forEach(n => {
    result.push(`* ${reduce(n, schema)}`);
  });
  return result.join('\n');
};
export default bulletList;