import { type PropsWithChildren } from 'react';
import { type SerializedStyles } from '@emotion/react';
import { type SmartLinkAlignment, type SmartLinkDirection, type SmartLinkPosition, type SmartLinkSize, type SmartLinkWidth } from '../../../../../constants';
export type ElementGroupProps = PropsWithChildren<{
    align?: SmartLinkAlignment;
    direction?: SmartLinkDirection;
    overrideCss?: SerializedStyles;
    size?: SmartLinkSize;
    width?: SmartLinkWidth;
    testId?: string;
    position?: SmartLinkPosition;
    className?: string;
}>;
