import React from 'react';
import { type UnresolvedViewProps } from './types';
declare const UnresolvedViewOld: ({ actions, cardState, children, onAuthorize, onClick, onError, showPreview, testId, title, url, }: UnresolvedViewProps) => React.JSX.Element;
export default UnresolvedViewOld;
