import React from 'react';
import { type IconProps as TempIconProps, type LogoProps as TempLogoProps } from '@atlaskit/temp-nav-app-icons/types';
import { type LogoProps } from './types';
/**
 * Creates a feature flagged component that renders the legacy logo or the new logo
 * based on the platform-logo-rebrand feature flag.
 *
 * @param LegacyComponent - The legacy logo component.
 * @param NewComponent - The new logo component.
 * @returns A feature flagged component that renders the legacy logo or the new logo.
 */
export declare const createFeatureFlaggedComponent: (LegacyComponent: React.ComponentType<LogoProps>, NewComponent: React.ComponentType<TempLogoProps> | React.ComponentType<TempIconProps>) => ({ size, ...props }: LogoProps) => React.JSX.Element;
/**
 * Creates a wrapper around the new logo or icon component to ensure it receives the correct default (medium) size prop.
 *
 * @param NewComponent - The new logo or icon component.
 */
export declare const tempSizeWrapper: (NewComponent: React.ComponentType<TempLogoProps> | React.ComponentType<TempIconProps>) => ({ size, ...props }: LogoProps) => React.JSX.Element;
export declare const LEGACY_ONLY_LOGOS: string[];
export declare const SHARED_LOGOS: string[];
export declare const NEW_ONLY_LOGOS: string[];
