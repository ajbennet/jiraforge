import React from 'react';
export interface PopupFooterProps {
    currentDisplayCount: number;
    totalCount: number;
    filterName: string;
}
declare const PopupFooterExported: (props: PopupFooterProps) => React.JSX.Element;
export default PopupFooterExported;
