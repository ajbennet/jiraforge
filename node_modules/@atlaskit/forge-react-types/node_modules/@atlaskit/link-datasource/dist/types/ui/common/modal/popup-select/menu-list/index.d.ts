import React from 'react';
import { type MenuListComponentProps } from '@atlaskit/select';
import { type SelectOption } from '../types';
export type CustomMenuListProps = {
    isError?: boolean;
    isLoading?: boolean;
    isLoadingMore?: boolean;
    isEmpty?: boolean;
    showMore?: boolean;
    handleShowMore?: () => void;
    filterName: string;
    errors?: unknown[];
};
export declare const CustomMenuListExported: (props: MenuListComponentProps<SelectOption, true>) => React.JSX.Element;
export default CustomMenuListExported;
