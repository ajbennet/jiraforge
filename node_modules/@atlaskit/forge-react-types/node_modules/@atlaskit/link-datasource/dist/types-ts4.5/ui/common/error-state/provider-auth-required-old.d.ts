import { jsx } from '@emotion/react';
import { type DatasourceMeta } from '@atlaskit/linking-types';
interface ProviderAuthRequiredProps {
    auth: DatasourceMeta['auth'];
    onAuthSuccess: () => void;
    onAuthError: () => void;
    extensionKey: string | null;
    providerName: DatasourceMeta['providerName'];
    datasourceId: string;
}
export declare const ProviderAuthRequiredOld: ({ auth, onAuthSuccess, onAuthError, extensionKey, providerName, datasourceId, }: ProviderAuthRequiredProps) => jsx.JSX.Element;
export {};
