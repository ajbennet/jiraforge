import React, { type ReactNode } from 'react';
export interface ContentContainerProps {
    children: ReactNode;
    withTableBorder?: boolean;
}
export declare const ContentContainerNew: ({ children, withTableBorder }: ContentContainerProps) => React.JSX.Element;
export declare const ContentContainer: (props: ContentContainerProps) => React.JSX.Element;
