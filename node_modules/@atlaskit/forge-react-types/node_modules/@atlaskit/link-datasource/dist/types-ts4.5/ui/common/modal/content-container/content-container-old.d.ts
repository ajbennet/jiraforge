import React, { type ReactNode } from 'react';
export interface ContentContainerProps {
    children: ReactNode;
    withTableBorder?: boolean;
}
export declare const ContentContainerOld: ({ children, withTableBorder }: ContentContainerProps) => React.JSX.Element;
