import { jsx } from '@emotion/react';
export type TableFooterProps = {
    datasourceId: string;
    itemCount?: number;
    onRefresh?: () => void;
    isLoading: boolean;
    url?: string;
};
export declare const TableFooterOld: ({ datasourceId, itemCount, onRefresh, isLoading, url, }: TableFooterProps) => jsx.JSX.Element | null;
