/// <reference types="react" />
import { type LogoSize } from './types';
type LogoWrapperProps = {
    appearance: 'brand' | 'neutral' | 'inverse' | 'legacy';
    size?: LogoSize;
    svg: string;
    customThemeSvg?: string;
    label: string;
    testId?: string;
    iconColor?: string;
    textColor?: string;
};
export declare function LogoWrapper({ appearance, size, svg, customThemeSvg, label, testId: userDefinedTestId, iconColor: customIconColor, textColor: customTextColor, }: LogoWrapperProps): JSX.Element;
export {};
